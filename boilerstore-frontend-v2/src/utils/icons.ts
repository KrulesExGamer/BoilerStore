/**
 * This module encapsulates all the static icons from Font Awesome.
 * You can store icons that will be heavily used many times on the site here.
 * Uncommon icons should be imported dynamically (do not use this module for that purpose).
 * @module icons
 */

/**
 * Object containing static icons from Font Awesome.
 * @typedef {Object.<string, import("@fortawesome/fontawesome-svg-core").IconDefinition>} IconsObject
 */

/** @type {IconsObject} */
const icons = {
    
};

export default icons;
